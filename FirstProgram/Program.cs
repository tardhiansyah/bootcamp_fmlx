using System;   // using System namspace

int x = 12 * 30;    // Statement 1

// * System[namespace] . Console[class] . WriteLine[method] (method params)
Console.WriteLine(x);    // Not needed to specify System (because already using the System)